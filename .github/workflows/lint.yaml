name: Linters

on:
  push:
    branches: [main, dev]
  pull_request:
    types: [opened, synchronize, reopened]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  ruff:
    uses: osint-forge/.github/.github/workflows/ruff.yaml@main
    secrets: inherit

  mypy:
    uses: osint-forge/.github/.github/workflows/mypy.yaml@main
    secrets: inherit
